# Website lessons
- A full square logo cannot be treated as a raven-only icon. Display the supplied lockup legibly without reconstructing the wordmark.
- Lifecycle and ordering availability are separate. A working build does not establish stock or sale terms.
- Every file under public is published, including unused screenshots. Review the directory as well as rendered pages.
- A static contact form cannot receive inquiries. Show truthful unavailable/draft behavior until a real route is configured; never claim delivery from a click.
- Build-time draft filtering must apply to pages, catalog, contact choices and client bundles.
- Local source evidence referencing private repositories belongs in an ignored authoring register, even though docs is outside the browser build: the site Git repository itself is public.


## 2026-09-15 — Commerce prepared locally, not launched

Added /shop/, /cart/ and /account/ using the existing BORGAS design. Authorized prices: BRC-02 USD 149.99, BFD-01 Generic USD 449.99. User is creating PayPal Business and asked to prepare Supabase for later. Shared catalog is shared/catalog.json. Cloudflare Worker/D1 server in commerce/ validates totals, discounts, authenticated ownership and PayPal captures/webhooks. No discounts, accounts, payment credentials or cloud resources have been created. Checkout defaults off. Shipping/tax/availability adapter, stock handling and sale terms remain launch dependencies; never default missing tax/shipping to zero. Read commerce/README.md before deployment. Browser cart and unavailable-service states checked; Astro check/build, 20 automated commerce checks, and Worker dry-run build passed. API tests use mocked providers, not real sandbox payments.

Cloudflare setup progressed: D1 borgas-orders created (c6a58eac-ea1f-4ab8-8d79-a9eee0a7315b), migration 0001 applied remotely. Worker borgas-commerce deployed at https://borgas-commerce.borgas-site.workers.dev with CHECKOUT_ENABLED=false and PAYPAL_ENV=sandbox. Version 9deb32f8-528c-4aa3-8cae-b64592fba068. Initial endpoint check encountered a TLS handshake failure; public endpoint verification remains outstanding. PAYPAL secret exists in private BORGAS-Master GitHub Actions; it has not been transferred to Cloudflare. User must add Cloudflare secret PAYPAL_SECRET; merchant ID, webhook and delivery adapter remain outstanding. Main website changes remain local and unpublished.

Cloudflare secret verified by name only: Paypal_Secret. Worker accepts that spelling and PAYPAL_SECRET. Public /config endpoint now verified responding with checkoutEnabled=false.

Sandbox integration test complete: passwordless customer sign-in succeeded with Supabase PKCE magic links (default free email template; editing templates requires custom SMTP or paid plan). Supabase Site URL is https://borgas.us and exact local callback http://127.0.0.1:4322/account/ is allowed. Authenticated /sandbox/check verified the PayPal Sandbox credentials and configured webhook URL/event. SANDBOX10 reduced BRC-02 by USD15; server test quote USD156.99 includes USD10 shipping and USD12 tax fixtures. The restricted Sandbox fallback opened PayPal approval in a normal tab when the embedded popup was blocked. PayPal captured test order 3A0205B8, the cart cleared, customer history displayed paid, and a read-only remote D1 query confirmed status=paid, payment_env=sandbox and a capture ID. No real money or shipment was involved. The checkout remains restricted by SANDBOX_TEST_EMAIL, SANDBOX_TEST_MODE=true and PAYPAL_ENV=sandbox; CHECKOUT_ENABLED stays false. 27 automated commerce checks, Astro check and the production frontend build passed. Migration 0002 marks order payment environments. Remove the Sandbox test flag before production setup. Production shipping/tax/stock, SMTP, sale terms and live PayPal credentials remain outstanding. The main-site frontend is not published yet.
