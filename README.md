# borgas-site

The BORGAS website, served by GitHub Pages. One static page and the mark.

`index.html` carries two values at the bottom that depend on the business
registration - the contact email and the legal name. Set them before the
domain is pointed here. Add a `CNAME` file containing the domain to bind it.
